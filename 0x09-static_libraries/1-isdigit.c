#include "main.h"

/**
 * _isdigit - checks for digit from 0 to 9
 *  @c: intiger needed to be checked
 * Return: 1 if c is digit or  0 if is not a digit
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

